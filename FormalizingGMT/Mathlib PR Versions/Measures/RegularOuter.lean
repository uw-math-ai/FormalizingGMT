

/-
**TODO (Theo and Annie)**: Include here the following parts of Project Versions/Measures/Basic, and start a PR to merge them into Mathlib:
1. definition of regular outer measure
2. Proposition 1.11.7 from Bogachev's book (if ready; else start a PR with the rest of the content)-/



/-**TODO (Nathan)**: Lemma: if `μ` is a regular outer measure on a space `X`, then for every `A ⊆ X` there exists a `μ`-measurable set `M` such that `A ⊆ M` and `μ(M \ A) = 0`.-/

/-**TODO (Nathan)**: Lemma: if `μ` is a regular outer measure on a space `X`, then for every `A ⊆ X`, `μ(A)` equals the infimum of `μ(M)` over all `μ`-measurable sets `M` containing `A`.-/
